/**
 * リプレイ本体アップロードの上限バイト数。
 *
 * リプレイは入力イベントの TOML テキストで、長い譜面でも通常は数百 KB に
 * 収まる。上限がないと認証済みアカウントから R2/blob への巨大 blob 連投で
 * ストレージ DoS が可能になるため、余裕を持たせた固定上限で拒否する。
 */
export const MAX_REPLAY_BYTES = 4 * 1024 * 1024
