BMZ Player Rust Dependency Licenses
===================================

This report is generated by cargo-about from Cargo.lock and crate metadata.
It covers the Rust crates resolved for the selected workspace, features, and
target triples. It is not legal advice.


License Overview
----------------

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}


License Texts
-------------

{{#each licenses}}
================================================================================
{{{name}}}
--------------------------------------------------------------------------------
Used by:
{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} ({{{crate.repository}}}){{/if}}
{{/each}}

{{{text}}}

{{/each}}
